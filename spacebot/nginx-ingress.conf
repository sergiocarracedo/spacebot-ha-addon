server {
    listen 8099 default_server;

    # Only allow HA Ingress proxy — deny all direct external access
    allow  172.30.32.2;
    deny   all;

    # WebSocket upgrade support
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $http_host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    proxy_read_timeout  86400s;
    proxy_send_timeout  86400s;
    proxy_redirect      off;

    # Spacebot reads window.__SPACEBOT_BASE_PATH to prefix all API calls and
    # asset URLs. We inject it into the HTML using the X-Ingress-Path header
    # that HA Ingress sends on every request.
    sub_filter_once off;
    sub_filter_types text/html;

    # Inject the base path script tag and a floating terminal button before </head>
    sub_filter '</head>' '<script>window.__SPACEBOT_BASE_PATH="$http_x_ingress_path";</script><style>#_ttyd-btn{position:fixed;bottom:20px;right:20px;z-index:99999;background:#1a1a2e;color:#00d4aa;border:1px solid #00d4aa;border-radius:8px;padding:8px 14px;font-family:monospace;font-size:13px;cursor:pointer;text-decoration:none;opacity:0.85;transition:opacity .2s}#_ttyd-btn:hover{opacity:1}</style><a id="_ttyd-btn" href="$http_x_ingress_path/terminal/" target="_blank" title="Spacebot container terminal" alt="Spacebot container terminal">&gt;_</a></head>';

    # Also rewrite static asset hrefs/srcs that are absolute in the HTML
    sub_filter 'src="/assets/'  'src="$http_x_ingress_path/assets/';
    sub_filter 'href="/assets/' 'href="$http_x_ingress_path/assets/';
    sub_filter 'href="/favicon' 'href="$http_x_ingress_path/favicon';

    # ttyd web terminal — proxied at /terminal/
    # ttyd requires WebSocket support (same headers as main proxy).
    location /terminal/ {
        proxy_pass http://127.0.0.1:7681/terminal/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $http_host;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    location / {
        proxy_pass http://127.0.0.1:19898;
    }
}
